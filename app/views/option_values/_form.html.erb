<%= form_with(model: option_value) do |form| %>
  <% if option_value.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(option_value.errors.count, "error") %> prohibited this option_value from being saved:</h2>

      <ul>
        <% option_value.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :option_type_id, style: "display: block" %>
    <%= form.select :option_type_id, OptionType.all.collect { |ot| [ot.name, ot.id] }, include_blank: true %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
