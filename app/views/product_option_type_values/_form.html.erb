<%= form_with(model: product_option_type_value) do |form| %>
  <% if product_option_type_value.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_option_type_value.errors.count, "error") %> prohibited this product_option_type_value from being saved:</h2>

      <ul>
        <% product_option_type_value.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
